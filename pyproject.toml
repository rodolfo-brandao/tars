[project]
name = "tars"
version = "0.1.0"
description = "My first -- and currently only -- Discord bot, made to discover movies online."
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "discord-py>=2.7.1",
    "python-dotenv>=1.2.2",
    "requests>=2.34.2",
]
